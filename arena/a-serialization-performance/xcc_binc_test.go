package serialization

import (
	"testing"
)

// these tests are invoked multiple times by the benchmarking framework

func Benchmark___Serialization___Binc(t *testing.B) {
	//TODO  https://pkg.go.dev/goa.design/goa/encoding/binc#section-sourcefiles

	// datasourceArrayLength := len(arena.Datasource)

	// for i := 0; i < t.N; i++ {
	// 	x := arena.Datasource[i%datasourceArrayLength]

	// 	_, err := binc.Marshal(x)
	// 	if err != nil {
	// 		panic(err)
	// 	}
	// }
}
